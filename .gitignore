# Local, machine-specific Claude Code settings (permission allowlists etc.)
.claude/settings.local.json

# Playwright MCP run artifacts (traces, console logs, page snapshots)
.playwright-mcp/

# Locally installed third-party Claude Code skills (not part of this project)
.claude/skills/
skills-lock.json

# Security scan reports (describe weaknesses; never publish)
security-reports/
